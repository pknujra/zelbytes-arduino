#ifndef CONFIG_H
#define CONFIG_H

// Pin Assignments
const uint8_t LED_PIN = 13;    // Built-in LED
const uint8_t BTN_PIN = 2;     // Push Button (Active-Low)

// Debounce Configuration
const unsigned long DEBOUNCE_MS = 50;

#endif

